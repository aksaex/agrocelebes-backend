version: "3.8"
services:
  test-backend-agro:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: agrocelebes_test_auth
    environment:
      - NODE_ENV=test
      - JWT_SECRET=test_secret_key_123
    restart: "no"